import React from 'react'
import "./common.css"

const HeaderTitle = ({title}) => {
  return (
    <div className='header-title'>
      <span>{title}</span>
    </div>
  )
}

export default HeaderTitle
